# 圓餅圖顯示優化說明

## 問題描述

當財務報表中的分類數量很多，且每個分類的金額比較分散時，圓餅圖會出現以下問題：

1. **標籤擁擠**：所有分類的名稱都擠在一起，無法清楚辨識
2. **視覺混亂**：太多小扇區，整體圖表難以閱讀
3. **資訊過載**：重要分類被淹沒在眾多小分類中

### 範例
```
飲食 45%
交通 20%
娛樂 15%
醫療 8%
服飾 5%
書籍 3%    ← 這些小分類的標籤
文具 2%    ← 會全部擠在一起
其他 2%    ← 無法清楚閱讀
```

---

## 解決方案

### 1. 智能合併小分類

**規則**：
- 將佔比 < 5% 的所有分類合併為「其他」
- 合併閾值可自訂（預設 5%）

**效果**：
```
飲食 45%
交通 20%
娛樂 15%
醫療 8%
服飾 5%
其他 7%    ← 書籍(3%) + 文具(2%) + 其他(2%)
```

### 2. 條件式標籤顯示

**規則**：
- 只有佔比 >= 5% 的扇區才在圖表上顯示標籤
- 扇區內顯示百分比，格式：`XX%`
- 小於 5% 的扇區不顯示標籤，避免重疊

**視覺效果**：
- 大扇區清晰標示百分比
- 小扇區保持乾淨，不顯示標籤
- 透過圖例和 Tooltip 查看完整資訊

### 3. 增強互動式 Tooltip

**懸停顯示資訊**：
- 分類名稱
- 金額（格式化為千分位）
- 佔比（百分比）

**「其他」分類特別處理**：
當滑鼠懸停在「其他」扇區時，會展開顯示所有被合併的小分類：

```
其他
金額: NT$ 7,000
佔比: 7%
─────────
包含：
書籍: 3%
文具: 2%
其他: 2%
```

### 4. 底部圖例

**顯示內容**：
- 所有分類（包含「其他」）
- 格式：`分類名稱 (佔比%)`
- 彩色圓點標記對應顏色

**優點**：
- 提供完整的分類資訊
- 不會與圖表標籤衝突
- 方便快速瀏覽所有分類

---

## 技術實現

### 新增組件：SmartPieChart

```typescript
interface SmartPieChartProps {
  data: ChartDataItem[];
  height?: number;           // 圖表高度，預設 300
  mergeThreshold?: number;   // 合併閾值（%），預設 5.0
}
```

**使用範例**：

```tsx
import { SmartPieChart } from "@/app/components/SmartPieChart";

// 基本使用
<SmartPieChart data={chartData} />

// 自訂高度和閾值
<SmartPieChart 
  data={chartData} 
  height={400} 
  mergeThreshold={3.0}  // 改為 3% 合併
/>
```

### 資料格式

```typescript
interface ChartDataItem {
  name: string;        // 分類名稱
  value: number;       // 金額
  percentage: string;  // 百分比字串，例如 "15.5"
}
```

### 應用範圍

已套用在以下頁面的圓餅圖：

1. **Dashboard 頁面**：
   - `CategoryPieChart` - 本月支出分類

2. **報表頁面**：
   - 本月支出分類佔比
   - 全年支出分類佔比

---

## 視覺效果對比

### 優化前
```
問題：
❌ 10+ 個分類標籤擠在一起
❌ 小扇區難以點擊和辨識
❌ 視覺混亂，重點不突出
```

### 優化後
```
改善：
✅ 只顯示主要分類（>= 5%）的標籤
✅ 小分類合併為「其他」，簡潔清晰
✅ 懸停查看詳細資訊，包含「其他」明細
✅ 底部圖例提供完整資訊
```

---

## 自訂設定

### 調整合併閾值

如果你想改變小分類的合併標準：

```tsx
// 更嚴格：只顯示佔比 >= 10% 的分類
<SmartPieChart data={chartData} mergeThreshold={10.0} />

// 更寬鬆：顯示佔比 >= 3% 的分類
<SmartPieChart data={chartData} mergeThreshold={3.0} />

// 不合併：顯示所有分類（設為 0）
<SmartPieChart data={chartData} mergeThreshold={0} />
```

### 建議閾值

| 分類數量 | 建議閾值 | 說明 |
|---------|---------|------|
| 5 個以下 | 0% | 不需要合併 |
| 5-10 個 | 3-5% | 輕度合併 |
| 10-15 個 | 5-8% | 中度合併（預設） |
| 15 個以上 | 8-10% | 重度合併 |

---

## 顏色配置

使用 9 種區分度高的顏色循環使用：

```javascript
const COLORS = [
  "#3b82f6", // blue - 藍色
  "#10b981", // green - 綠色
  "#f59e0b", // amber - 琥珀色
  "#ef4444", // red - 紅色
  "#8b5cf6", // violet - 紫色
  "#ec4899", // pink - 粉色
  "#06b6d4", // cyan - 青色
  "#f97316", // orange - 橘色
  "#64748b", // slate - 灰色（用於「其他」）
];
```

**特別說明**：
- 「其他」分類固定使用灰色 (#64748b)
- 其他分類按順序循環使用前 8 種顏色

---

## 測試建議

### 測試場景 1：少量分類（< 5 個）
- **預期**：所有分類都顯示標籤
- **驗證**：不應該出現「其他」分類

### 測試場景 2：中量分類（5-10 個）
- **預期**：主要分類顯示標籤，小分類合併
- **驗證**：「其他」分類的 Tooltip 能正確展開

### 測試場景 3：大量分類（10+ 個）
- **預期**：大部分分類被合併為「其他」
- **驗證**：圖表清晰易讀，不會有標籤重疊

### 測試場景 4：平均分布（所有分類佔比相近）
- **預期**：可能所有分類都 < 5%，全部合併為「其他」
- **驗證**：應調整閾值或檢視資料是否合理

---

## 注意事項

1. **資料品質**：
   - 確保傳入的 `percentage` 是有效的數字字串
   - 如果資料為空陣列，會顯示「尚無資料」

2. **效能考量**：
   - 合併邏輯在前端執行，不影響後端 API
   - 適用於 100 個以內的分類數量

3. **響應式設計**：
   - 圖表會自動適應容器寬度
   - 建議容器最小寬度 300px

4. **Dark Mode**：
   - Tooltip 自動適應深色模式
   - 使用 Tailwind 的 `dark:` 前綴

---

## 未來改進方向

1. **使用者自訂閾值**：
   - 在設定頁面讓使用者選擇合併閾值
   - 儲存到使用者偏好設定

2. **可展開圓餅圖**：
   - 點擊「其他」扇區可展開顯示子分類
   - 實現多層級圓餅圖（Drill-down）

3. **匯出功能**：
   - 匯出圖表為圖片
   - 包含「其他」分類的完整明細

4. **動畫效果**：
   - 加入過渡動畫
   - 提升視覺體驗

---

## 參考資源

- [Recharts 官方文件](https://recharts.org/)
- [Tailwind CSS 顏色系統](https://tailwindcss.com/docs/customizing-colors)
- [資料視覺化最佳實踐](https://www.data-to-viz.com/)
